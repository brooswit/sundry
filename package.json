{
  "name": "@brooswit/sundry",
  "version": "0.2.0",
  "description": "A toolbox of small, well-tested TypeScript utilities",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brooswit/sundry.git"
  },
  "homepage": "https://github.com/brooswit/sundry#readme",
  "bugs": {
    "url": "https://github.com/brooswit/sundry/issues"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "bun run scripts/load/generate.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --sourcemap=external && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "test:unit": "bun test test/unit test/load && bun run scripts/coverage/gate.ts",
    "release:check": "bun run scripts/release/check.ts",
    "check": "bun run generate && bun run typecheck && bun test test/unit test/load && bun run scripts/coverage/gate.ts && bun run scripts/verify-generated-is-committed.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1"
  }
}
